export const agentNames = ["CEO Agent", "Research Agent", "Creator Agent", "Clipper Agent", "Affiliate Agent", "Publishing Agent", "Analytics Agent", "Policy Agent"] as const;
export type AgentName = typeof agentNames[number];
export type OrchestrationInput = { objective: string; platform: string; niche: string; targetAudience: string; contentType: string; language?: string; transcript?: string; sourceTitle?: string; productName?: string; productCategory?: string; metrics?: { views?: number; likes?: number; comments?: number; shares?: number; saves?: number; watchTime?: number; retentionRate?: number; clicks?: number; conversions?: number; revenue?: number } };
export type AgentExecutionLog = { agent_name: AgentName; start_time: string; end_time: string; duration: number; status: "completed" | "fallback" | "failed" | "skipped"; result_summary: string };
export type CeoExecutionPlan = { execution_plan: Array<{ step: number; agent: AgentName; objective: string }>; selected_agents: AgentName[]; priority_order: AgentName[]; expected_output: string[]; risks: string[]; next_steps: string[] };
export type PolicyReview = { risk_level: "low" | "medium" | "high"; disclosure_recommendation: boolean; originality_notes: string[]; policy_notes: string[] };
export type OrchestrationOutput = { workflow_id: string; execution_plan: CeoExecutionPlan["execution_plan"]; selected_agents: AgentName[]; priority_order: AgentName[]; outputs: Record<string, unknown>; recommendations: string[]; risks: string[]; next_actions: string[]; execution_logs: AgentExecutionLog[]; status: "completed" | "completed_with_fallback" };
