ALTER TYPE "SocialConnectionStatus" ADD VALUE IF NOT EXISTS 'DISABLED';

DO $$ BEGIN
  CREATE TYPE "UploadMethod" AS ENUM ('MANUAL', 'API', 'BROWSER_AUTOMATION');
EXCEPTION
  WHEN duplicate_object THEN null;
END $$;

ALTER TABLE "SocialAccount"
  ADD COLUMN IF NOT EXISTS "uploadMethod" "UploadMethod" NOT NULL DEFAULT 'MANUAL',
  ADD COLUMN IF NOT EXISTS "notes" TEXT NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS "lastActivityAt" TIMESTAMP(3),
  ADD COLUMN IF NOT EXISTS "disabledAt" TIMESTAMP(3);
